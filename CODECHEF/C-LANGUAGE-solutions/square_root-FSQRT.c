#include<stdio.h>
#include<math.h>

int main (void)
{
   	int t;
   	scanf("%d",&t);
  	while(t--)
   	{
   		int a,b;
   		scanf("%d",&a);
   		b=sqrt(a);
   		printf("%d\n",b);

   	}
}
